/**
 * 隱私政策頁面的中央設定常數。
 *
 * 修改隱私政策內容時，同一個 PR 內更新此常數與對應的 i18n 文案，
 * 讓頁面上的「最後更新日期」與實際文字保持一致。
 *
 * 聯絡管道（GitHub Issues URL）定義於 i18n `privacy.contact.issueUrl`，
 * 因為四語系各自含有 label 文字需要翻譯，此處不再單獨匯出。
 */

/** 最近一次實質內容更新日期（ISO 8601 `YYYY-MM-DD` 字串）。 */
export const PRIVACY_POLICY_LAST_UPDATED = "2026-04-18";
